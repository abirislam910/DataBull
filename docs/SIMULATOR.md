# Sensor Simulator

*Placeholder. Documents the APScheduler-driven sensor simulator and reproducible seeding.*

See `/docs/SPEC.md` → "Sensor simulator" for the generation model (baseline + sinusoidal
variation + Gaussian noise + 1% spike chance).

## Reproducible seeding

To be documented: how to seed the simulator with a fixed `numpy` seed for deterministic
tests.
